#include <stdio.h>
int main()
{
    int n;
    printf("Enter The Number");
    scanf("%d", &n);
    if (n > 0)
        printf("+ve Number");
    else
        printf("-ve Number");
    return 0;
}

// Assigment 2
// Q=>2 Write a program to check whether given number is positive or negative?