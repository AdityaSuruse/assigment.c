#include <stdio.h>
int main()
{
    int n;
    printf("Enter The Number");
    scanf("%d", &n);
    if (n >= 1)
        printf("Positive Number");
    return 0;
}

// Assigment 2
// Q 1==>Write a program to check whether given number is positive?